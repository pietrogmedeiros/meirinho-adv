# Copie para .env e ajuste. Tudo tem padrão que funciona sem credencial
# externa: sem definir nada, o pipeline roda em modo mock de ponta a ponta.

# ---- Obrigatórias em servidor (os padrões só servem para rodar local) ----
# Segredo que assina as sessões. Gere com: openssl rand -hex 32
JWT_SECRET=troque-este-segredo-em-producao
# Senhas do banco: use só letras e números (elas entram numa URL de conexão).
# Valem na PRIMEIRA subida; depois disso, trocar exige ALTER ROLE no banco.
POSTGRES_PASSWORD=meirinho
APP_DB_PASSWORD=meirinho_app
# Armazenamento dos áudios. A senha precisa ter ao menos 8 caracteres.

# Conta de demonstração com dados (demo@meirinho.dev / meirinho123).
SEED_DEMO=false
# Porta do gateway na sua máquina (docker-compose.override.yml). No Easypanel
# não se aplica: o domínio aponta para o serviço gateway, porta 80.
GATEWAY_PORT=8080

LOG_LEVEL=info

# Análise e classificação. ANALYZER=claude exige ANTHROPIC_API_KEY; sem chave
# o serviço avisa no log e continua no mock em vez de morrer.
ANALYZER=mock
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

# Transcrição. whisper exige o binário instalado na imagem.
TRANSCRIBER=mock

# Monitoramento de processos.
PROCESS_PROVIDER=mock
POLL_INTERVAL=5m
# Chance de o mock gerar andamento por consulta (1 = sempre, para demonstrar o fluxo).
MOCK_MOVEMENT_CHANCE=0.35

# Regra conservadora do classificador: abaixo desta confiança a urgência é
# elevada ao piso e a movimentação é marcada como fallback.
CLASSIFY_PISO=media
CLASSIFY_MIN_CONFIANCA=0.6

# Notificação. Abaixo desta urgência não gera alerta — exceto fallback.
NOTIFY_URGENCIA_MINIMA=baixa
NOTIFY_EMAIL_ENABLED=false
NOTIFY_WHATSAPP_ENABLED=false

